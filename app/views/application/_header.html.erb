<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
  <div class="container">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <% if user_signed_in? %>
          <%= link_to '掲示板', rooms_index_path, class: 'navbar-brand' %>
        <% else %>
          <%= link_to '掲示板', new_user_session_path, class: 'navbar-brand' %>
        <% end %>
      </ul>
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to '掲示板作成', rooms_new_path, class: 'nav-link' %>
          </li>
        <% end %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            ユーザー
          </a>
          <% if user_signed_in? %>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <%= link_to 'プロフィール編集', edit_user_registration_path, class: 'dropdown-item' %>
              <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
            </div>
          <% else %>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <%= link_to 'ユーザー作成', new_user_registration_path, class: 'dropdown-item' %>
              <%= link_to 'サインイン', new_user_session_path, class: 'dropdown-item' %>
            </div>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>